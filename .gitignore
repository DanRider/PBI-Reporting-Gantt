# Node
node_modules/
npm-debug.log*

# pbiviz build artifacts (intermediate)
.tmp/
webpack.statistics.prod.html

# PBI Desktop per-user state inside any fixtures
**/.pbi/cache.abf
**/.pbi/localSettings.json
**/.pbi/editorSettings.json

# Sandbox / IDE
.vscode/
*.DS_Store

# Build artifacts — regenerable. Never commit (per pbi-custom-visual-hackery primer).
dist/*.pbiviz
